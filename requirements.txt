Flask==2.3.3
groq==0.9.0
python-dotenv==1.0.0
gunicorn==21.2.0
--only-binary :all:  # This line MUST stay
scikit-learn==1.5.2  # Changed to an existing version
numpy==1.26.4        # Updated to a compatible version
joblib==1.4.2        # Updated to a compatible version
